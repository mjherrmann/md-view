/** Default sidebar width in CSS pixels (17rem × 16px base). */
export const DEFAULT_WIDTH = 272

/** Minimum sidebar width in CSS pixels. */
export const MIN_WIDTH = 120

/** Maximum sidebar width as a ratio of application body width. */
export const MAX_WIDTH_RATIO = 0.5

/** Width change per arrow key press in CSS pixels. */
export const KEYBOARD_STEP = 10

/** Width change per Shift+arrow key press in CSS pixels. */
export const KEYBOARD_STEP_LARGE = 50

/** Viewport width at or below which mobile layout applies (CSS pixels). */
export const MOBILE_BREAKPOINT = 768
